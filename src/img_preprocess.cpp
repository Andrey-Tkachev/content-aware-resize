#include "img_preprocess.h"
#include <opencv2/opencv.hpp>

namespace preprocess {
    void gradient(cv::Mat& in, cv::Mat& out, blur_extent ext) {
        // TODO calc gradient image using Sobel operator (cv::Sobel)
    }
}
